import * as mockTest from './mock/test'
import * as mockUser from './mock/user'
import * as mockMessage from './mock/message'
import * as mockContact from './mock/contact'
import * as socket from './socket'
import * as user from './request/user'

export default {
    mockTest, mockUser, mockMessage, mockContact, socket, user
}